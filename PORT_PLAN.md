# План порта reSF2 → 1:1 с оригиналом

Цель: движок, который загружает ассеты и ведёт себя байт-в-байт как
оригинальная мобильная версия Shadow Fight 2 (APK v1.9.21, Marmalade SDK +
Cocos2d-x 2.x). Приоритет источников: Marmalade (`ShadowFight2.s86` /
`ShadowFight2_android.bin`), затем Unity IL2CPP (`libil2cpp.so` v2.46).

## Правила работы

1. За один запуск закрывается **минимум один пункт целиком** — со сборкой,
   проверкой и коммитом. Полупункты не засчитываются.
2. Каждое изменение помечается `[ORIGINAL]` с адресом/символом бинарника либо
   `[HEURISTIC-TODO]`. Третьего не дано.
3. Никаких констант, подобранных на глаз, без явной пометки и объяснения, из
   какого измерения они получены.
4. Пункт считается закрытым только при наличии **проверки**: тест, скриншот
   против эталона или численное сличение с бинарником.

## Легенда статусов

`[ ]` не начат · `[~]` в работе · `[x]` закрыт

---

## Фаза 0 — Фундамент (загрузка данных)

- [x] **0.1 derbh (.dz) контейнер и кодеры.**
  Контейнер DTRZ, `DZ Coder` (0x004), `ZLib Coder` (0x008) — нативно.
  Проверка: 120/120 `files.dz`, 557/557 `animations.dz`, байт-в-байт.
  Разбор: `engine/reverse/dz/README.md`. Коммит `d46c2b6`.

- [ ] **0.2 Монтирование ассетов 1:1.**
  Сейчас `read_file()` смотрит файловую систему раньше архивов, а движок
  зависит от пред-распакованных копий в `assets/files/`, `assets/animations/`.
  Надо: порядок резолва как в оригинале (`DzipFile::open` @ `0x102c9778`
  хранит полный путь директории в записи), убрать распакованные дубликаты.
  Проверка: игра стартует при наличии только `.dz` + россыпи из APK.

- [ ] **0.3 Отказ от `assets/` в пользу дерева APK.**
  Оригинал читает `assets/1536/...` и `assets/768/...` по тиру устройства
  (`devices.xml`: `DefaultResolution`, `LocationResolution`). Сейчас движок
  читает плоский `assets/locations/dojo/`. Надо выбирать тир как оригинал.
  Проверка: смена тира меняет загружаемые атласы, логи это показывают.

## Фаза 1 — Система координат и камера

- [x] **1.1 Единая система координат + инструментальная проверка.**
  Оверлей `--debug-world` (F1 переключает) рисует в мировых координатах оси,
  границы `Width`×`Height`, полосу масок, две независимые оценки плоскости
  пола, маркеры NPivot и самой нижней точки бойца, маркер противника — плюс
  числовой вывод. Реализация: `Game::render_debug_world()`.

  **Замеры на dojo (1280×720), первый запуск:**

  ```
  params  Width=1960 Height=560 Wall=305 Floor=80
  view    zoom=1.2857  visible 996 x 560 world units
  camera  x=-380.9 y=0.0
  mask band  top=226 bottom=-220  height=446
  floor A (-H/2+Floor)=-200   B (layer_3 top)=-193  delta=7
  player  pivot=(-457, -75)  y_adjust=18.2  lowest node=-331
  player  foot-vs-floorA=-131  foot-vs-floorB=-138
  enemy   pos=(-305, -105)   params X=973 -> X-W/2=-7
  ```

  **Что это дало:**

  1. **Система координат подтверждена.** Две независимые оценки пола сходятся
     в пределах 7 единиц: `-Height/2 + Floor = -200` и верхняя кромка полосы
     `layer_3` = `-193`. Значит `Floor="80"` — это нижний отступ от границы
     мира, а модель «`<Image>` центрированы и Y-инвертированы» верна.
  2. **Настоящий баг найден и измерен: боец проваливается сквозь пол на
     131–138 единиц** (нижняя точка `-331` против пола `-193..-200`). Это не
     камера. Прежняя оценка «~87 единиц на глаз» была неверной — реальное
     расхождение в полтора раза больше.
  3. **Локализовано до данных анимации.** `resolve_body_node` считает
     `sy = world_cy + (ly - pivot_local_y)` при масштабе 1.0. Из
     `sy = -331`, `world_cy = -75`, `pivot_local_y = 169.48` следует
     `ly = -86.5` — то есть анимация даёт ступне **отрицательный** Y в
     model-space, хотя в покое `NToe ≈ 65.5`. В кадре анимации ступня
     оказывается на 256 единиц ниже пивота вместо 104 — ровно в 2.46 раза.
     Подозрение: разбор `.bin` (единицы/шаг) либо пропущенный масштаб.
  4. **Побочная находка:** `enemy_pos_x_ = -305`, хотя из params следует
     `973 - 980 = -7`. Значит позицию противника кто-то переписывает после
     инициализации. Отдельная задача, см. 5.3.
  5. Видимая полоса по маскам — 446 из 560. Судить о кадре нельзя, пока не
     починен пункт 3.1.

- [ ] **1.2 Точный закон камеры.**
  `Fight` (`FUN_100b3860`): `Location*` в `+0x24c`, встроенный `Camera` в
  `+0x250` (ctor `FUN_10070270`). У камеры слоты `"Camera"`/`"Position"` —
  позиция гонится через систему анимации. Найти вертикальную видимую полосу
  (маски `pixel_1` оставляют 446 из `Height=560`; вероятно участвуют
  `Wall=305` / `Floor=80`) и закон следования по X.
  Проверка: кадр совпадает с эталонным скриншотом первого запуска.

- [ ] **1.3 Многоразрешённость.**
  Поведение при других соотношениях сторон и размерах окна — как в оригинале
  (маски закрывают перескан, playfield не растягивается).
  Проверка: скриншоты 16:9, 4:3, 20:9 без артефактов.

## Фаза 2 — Рендер сцены

- [ ] **2.1 Параллакс слоёв.**
  `Layer Factor` (0.1 / 0.4 / 1.0) — точная формула смещения фона от камеры.
  Проверка: движение камеры даёт те же относительные сдвиги, что в оригинале.

- [ ] **2.2 `SimpleEffect` / `Transparency`.**
  Анимация прозрачности по точкам (`Period`/`Value`/`Ease`) — сейчас не
  реализована; в dojo это мерцание светового пятна `layer_4`.
  Проверка: период и амплитуда совпадают с params.

- [ ] **2.3 Порядок отрисовки и маски.**
  Слои `Type=1` (спрайты) и `Type=2` (ModelsViewer) в правильном порядке,
  маски поверх.

## Фаза 3 — Модель бойца

- [ ] **3.1 Боец проваливается сквозь пол на 131–138 единиц.** ← *следующий*
  Измерено оверлеем из 1.1. Ступня в кадре анимации оказывается на 256 единиц
  ниже пивота вместо 104 (в 2.46 раза). `resolve_body_node` работает с
  масштабом 1.0, значит источник — либо разбор `.bin` (единицы/шаг кадра),
  либо потерянный масштаб модели. Начинать с дампа сырых координат узлов
  первого кадра `stance_2` и сличения с `skeleton_min.xml` в покое.
  Проверка: для стойки / приседа / прыжка / переката нижняя точка бойца
  лежит на плоскости пола ±1 единица (оверлей это показывает числом).

- [ ] **3.2 Слои модели и подмена частей.**
  `assets/models/*.xml` — тело, броня, шлем, оружие. Сейчас грузится только
  часть. Порядок слоёв и точки крепления.

- [ ] **3.3 Cloth / MacroNode.**
  В `body.xml` есть `Cloth="1"` и `MacroNode` с весами `LCC1..4` — физика
  ткани оригинала. Сейчас не реализована.

## Фаза 4 — Анимация

- [ ] **4.1 Формат .bin и воспроизведение.**
  Сверить с `ModelAnimation::playInfo` (`FUN_101661d0`, кандидат уже помечен
  GREEN в Phase 2 репозитория).

- [ ] **4.2 Приоритеты, переходы, MidFrames/FirstFrame.**
  `moves.xml`: transitions между движениями. Сейчас не реализовано.

- [ ] **4.3 Root motion.**
  Сейчас есть, но не сверен с оригиналом численно.

## Фаза 5 — Бой

- [ ] **5.1 Система условий (`Condition*`).**
  Уже есть заготовки в `engine/reverse/staged/` (ConditionInterval,
  ConditionCurrentAnimation). Довести до полной 1:1.

- [ ] **5.2 Хитбоксы, урон, блок.**
  `DamageFactor`, `BlockDamageFactor` из `internalSettings.xml`.

- [ ] **5.3 AI противника.**
  `assets/tactics/*.atf`, `tacticSettings.xml`, `ComputerSettings.xml`.
  Сейчас есть только груша.

- [ ] **5.4 Оружие: magic / ranged.**
  Сейчас только кулаки.

## Фаза 6 — UI

- [ ] **6.1 HUD верхней панели.**
  Звезда+уровень, полоса энергии, монеты, самоцветы, кнопка «+».
  Верстка из оригинальных атласов, не подбором. Сейчас — отладочный текст.

- [ ] **6.2 Свиток «МЕНЮ» и диалоговый свиток.**
  Первый запуск начинается с диалога сэнсэя.

- [ ] **6.3 Экранные контролы.**
  Виртуальный джойстик и кнопки удара/пинка (в оригинале — тач).

- [ ] **6.4 Локализация.**
  `assets/localizations/*.xml`, 12 языков.

- [ ] **6.5 Остальные экраны.**
  Карта, магазин, результаты, настройки.

## Фаза 7 — Прочее

- [ ] **7.1 Звук.** Сейчас заглушки. MP3 + `ActionRandomSound`/`ActionSound`.
- [ ] **7.2 Сохранения.** `usersDefault.xml` / `userSettings.xml`.
- [ ] **7.3 Прогрессия.** `stages.xml`, `quests.xml`, `perks.xml`, `forge.xml`.
- [ ] **7.4 55 из 56 локаций.** Сейчас только dojo.

## Фаза 8 — Уборка

- [ ] **8.1 Удалить артефакты опровергнутой LZMA-гипотезы.**
  `engine/reverse/dz_decoder.*`, тесты `test_dz_first_byte`,
  `test_dz_prob_layout`, `test_dz_range_settings`, `test_dz_decode`.
- [ ] **8.2 Удалить `game_old.hpp`.** Дубликат `game_clean.hpp` на 5700 строк.
- [ ] **8.3 Прогнать `game_clean.hpp` на предмет подогнанных констант.**
