587:        // BUT: don't flip during attacks/special moves (hit_anim_ > 0, move_state_ >= 10).
629:                        hit_anim_, is_uninterrupt_ ? 1 : 0, move_state_, current_move_.c_str());
696:                hit_anim_ = (uint32_t)(fc * 1000.0f / 20.0f);
734:                        // (hit_anim_ > 0 means attack in progress)
811:                hit_anim_ = (uint32_t)(fc * 1000.0f / 20.0f);
895:                    hit_anim_ = (uint32_t)(fc * 1000.0f / 20.0f);
1023:        if (hit_anim_ > 0) {
1024:            hit_anim_ -= std::min<uint32_t>(hit_anim_, dt);
1032:        if (move_state_ == 10 && hit_anim_ == 0) {
1065:        if (hit_anim_ > 0 && !current_move_.empty()) {
1092:        // hit_anim_ countdown already done above (before special move exit).
1093:        // Only do hit detection here if hit_anim_ is still > 0.
1095:        // changes), not just when hit_anim_ reaches 0.
1129:        if (hit_anim_ > 0 && bag_model_ && location_) {
1223:            if (hit_anim_ == 0) {
1230:        // IMPORTANT: Also clear current_move_ when hit_anim_ reaches 0
1234:        if (hit_anim_ == 0 && !current_move_.empty()) {
1401:            hit_anim_ = (uint32_t)(fc * 1000.0f / 20.0f);
3617:    int hit_anim_ = 0;    // ms remaining
