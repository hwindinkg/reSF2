<UserControl x:Class="Marmalade.App.ErrorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Marmalade.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="800" d:DesignWidth="480">

    <Grid x:Name="ErrorGrid" VerticalAlignment="Center" HorizontalAlignment="Center" Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="_errorText" Text = "Error Text"  HorizontalAlignment="Stretch" 
                   VerticalAlignment="top" Height="Auto" Margin="10,10,10,0" MaxHeight="400" TextWrapping="Wrap">
        </TextBlock>
        <Button Grid.Row="1" x:Name="_btnContinue" Content="Continue" HorizontalAlignment="Left" VerticalAlignment="bottom" Width="145" Height="80" Margin="0"/>
        <Button Grid.Row="1" x:Name="_btnStop" Content="Stop" HorizontalAlignment="Left" VerticalAlignment="bottom" Width="120" Height="80" Margin="145,0,0,0"/>
        <Button Grid.Row="1" x:Name="_btnIgnore" Content="Ignore" HorizontalAlignment="Left" VerticalAlignment="bottom" Width="120" Height="80" Margin="265,0,0,0"/>
    </Grid>
</UserControl>
