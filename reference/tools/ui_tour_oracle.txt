# Canonical oracle fidelity tour (phase1 step9).
# Requires the post-tutorial seed: reference/runner/index.html installs a
# Tutorial="END" savegame before boot (ORACLE_POST_TUTORIAL = true). Without it
# the game boots into the blocking tutorial and the hub is unreachable.
#
# Reached/emitted here: dojo_hub, dojo_menu_open, map_zone1, fight_intro,
# fight_stance, fight_attack, fight_hit.
# The other hub destinations (Shop / Profile / Settings) have no in-game route
# back to the dojo nav, so they use short one-destination tours documented in
# reference/traces/oracle_matrix/MANIFEST.md.
#
# Run from the repo root:
#   shell\bin\Release\net9.0-windows\OracleShell.exe --input-script reference/tools/ui_tour_oracle.txt --timeout-ms 42000
wait 8000
shot dojo_hub
tap 180 98
wait 1500
shot dojo_menu_open
tap 140 250
wait 2500
shot map_zone1
tap 1130 580
wait 9000
shot fight_intro
wait 5000
shot fight_stance
wait 3000
tap 1135 540
wait 1200
shot fight_attack
wait 1200
shot fight_hit
