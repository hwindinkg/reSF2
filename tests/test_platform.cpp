// tests/test_platform.cpp
//
// TestPlatform implementation (header-only; this TU anchors the vtable).

#include "test_platform.hpp"

// Intentionally empty — all TestPlatform methods are inline in the header.
// This translation unit exists so the vtable for TestPlatform has a home.
